mutation Login($signInCredentials: CredentialsInput!) {
    signInWithEmail(input: {credentials: $signInCredentials}) {
        token
        user {
            id
            email
            firstName
            lastName
            middleName
            gender
            age
        }
    }
}
